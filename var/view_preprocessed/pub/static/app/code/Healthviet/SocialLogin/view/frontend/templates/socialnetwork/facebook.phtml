<?php if ($block->isFacebookEnabled()): ?> <div class="modal-facebook"></div><?php endif; ?> <script>    require(['jquery', 'healthvietSocialLogin'],        function ($, sociallogin) {            $(document).ready(function () {                $('.modal-facebook').bind('click', function () {                    var url = '<?= $block->getButtonUrl();?>';                    if (url.length) {                        sociallogin.display(url, 'Facebook', 600, 600);                    }                });            })        })</script>